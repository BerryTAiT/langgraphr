% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dsl.R
\name{lg_graph}
\alias{lg_graph}
\title{Start a new agent graph (full-authoring path)}
\usage{
lg_graph(id, state = list(), entry = NULL)
}
\arguments{
\item{id}{A unique name for the graph, used as its server-side id.}

\item{state}{A named list describing the state channels. Each entry is a
list with fields `type` ("str", "number", "boolean", "list" or "any"),
`reducer` ("overwrite" or "append") and an optional `description`.}

\item{entry}{The id of the entry node. Defaults to the first node added.}
}
\value{
A graph builder object (class `lg_graph_builder`) for use with
  [lg_add_node()], [lg_add_edge()] and [lg_compile()].
}
\description{
Creates a graph builder whose nodes are plain R functions. When compiled
with [lg_compile()], the flow between nodes is orchestrated by the hidden
LangGraph server while every node body executes in R.
}
\examples{
\dontrun{
g <- lg_graph("demo", state = list(count = list(type = "number")))
}
}
