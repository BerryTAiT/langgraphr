% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/agent.R
\name{lg_connect}
\alias{lg_connect}
\title{Connect to the hidden LangGraph server (assistant path)}
\usage{
lg_connect(port = getOption("langgraphr.port", 8123L), thread_id = NULL)
}
\arguments{
\item{port}{Port for the hidden server (default from the
`langgraphr.port` option, otherwise 8123).}

\item{thread_id}{Reuse an existing thread id to continue a conversation;
`NULL` generates a fresh one.}
}
\value{
An [LgAgent] object.
}
\description{
Starts the hidden Python server if it is not already running and returns
an [LgAgent]: a bundled assistant whose tools are plain R functions you
register with `$add_tool()`. Conversation memory is keyed by `thread_id`.
}
\examples{
\dontrun{
agent <- lg_connect()
agent$add_tool(my_function, description = "What my_function does")
res <- agent$invoke("Hello!")
res$content
}
}
