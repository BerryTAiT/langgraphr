% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/server.R
\name{lg_start_server}
\alias{lg_start_server}
\title{Start the hidden LangGraph server}
\usage{
lg_start_server(
  port = getOption("langgraphr.port", 8123L),
  wait = TRUE,
  timeout = getOption("langgraphr.timeout", 60L),
  logfile = NULL,
  proxy = c("auto", "system", "direct"),
  extra_env = NULL
)
}
\arguments{
\item{port}{Port for the server (default from the `langgraphr.port`
option, otherwise 8123).}

\item{wait}{Wait for the server to become healthy before returning?}

\item{timeout}{Seconds to wait for the server to become healthy.}

\item{logfile}{Where to write server logs. Defaults to a temp file;
pass a path to keep the log.}

\item{proxy}{Network route for the sidecar's model traffic: `"auto"`
(detect the system proxy), `"system"` (force the detected proxy) or
`"direct"` (bypass every proxy).}

\item{extra_env}{Named character vector of extra environment variables
for the sidecar process.}
}
\value{
The port, invisibly.
}
\description{
Spawns the bundled Python/FastAPI sidecar as a supervised background
process and waits until it answers `/health`. Called automatically by
[lg_connect()] and [lg_compile()]; only call it directly to pre-warm the
server or to control the network route.
}
