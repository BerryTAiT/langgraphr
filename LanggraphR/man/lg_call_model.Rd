% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/client.R
\name{lg_call_model}
\alias{lg_call_model}
\title{Call an OpenAI-compatible chat model directly from R}
\usage{
lg_call_model(
  messages,
  model = NULL,
  base_url = NULL,
  api_key = NULL,
  temperature = NULL,
  max_tokens = NULL,
  timeout = 120
)
}
\arguments{
\item{messages}{A list of message lists, each with `role` and `content`.}

\item{model}{Model name. Defaults to `LANGGRAPHR_MODEL`, else
"gpt-4o-mini".}

\item{base_url}{API base URL. Defaults to `LANGGRAPHR_BASE_URL`, else
OpenAI's endpoint.}

\item{api_key}{API key. Defaults to `LANGGRAPHR_API_KEY`, then
`OPENAI_API_KEY`.}

\item{temperature}{Optional sampling temperature.}

\item{max_tokens}{Optional maximum number of tokens to generate.}

\item{timeout}{Request timeout in seconds.}
}
\value{
The assistant's reply text (character scalar).
}
\description{
Sends a chat-completions request from R itself (no Python involved), so R
graph nodes and tools can use an LLM without leaving the process.
}
